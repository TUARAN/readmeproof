# Contributing

Please add a minimal Markdown fixture for parser changes and run `npm test`. Security changes to command execution should explain the threat model and preserve the rule that execution requires an explicit `readmeproof` fence marker plus `--run`.
